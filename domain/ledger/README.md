# Ledger (Phase 5)

This package will implement the double-entry bookkeeping core:
- accounts (assets/liabilities/revenue/expense)
- transactions (auditable journal groups)
- ledger_entries (debit/credit postings)
- posting engine enforcing invariants (debits == credits, no negative amounts, etc.)

